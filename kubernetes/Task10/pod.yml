apiVersion: v1
kind: Pod
metadata:
  labels:
    run: dev-nginx-pod
  name: dev-nginx-pod
  namespace: dev
spec:
  containers:
  - image: nginx:latest
    name: dev-nginx-pod
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
